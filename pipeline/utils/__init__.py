"""
Utility helpers for the pipeline (model creation, point cloud conversion).

This module exists so that pipeline steps can import utilities without
depending on the legacy scripts directory.
"""


