Legacy utilities (kept for regeneration workflows not handled by the main pipeline):

- `create_mean_shape_from_pointclouds.py`: builds a mean shape (no PCA) from Biwi point clouds.
- `triangulate_pointcloud.py`: Poisson/Delaunay helpers to generate faces from point clouds.
- `heuristic_create_landmark_mapping.py`: legacy landmark→vertex mapper using geometric heuristics.
- `center_mesh.py`: recenters a mesh at the origin (optional).

